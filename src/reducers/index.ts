import auth from './auth';
import modal from './modal';
import products from './products';
import cart from './cart';

const rootReducer = { auth, modal, products, cart };

export default rootReducer;
